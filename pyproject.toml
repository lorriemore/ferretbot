[project]
name = "ferretbot"
version = "0.1.0"
description = "A loosely mustelid-themed Discord bot written with discord.py"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "discord>=2.3.2",
    "python-dotenv>=1.2.2",
]
